services:
  frontend:
    build:
      context: ./Front
      dockerfile: Dockerfile
    volumes:
      - ./Front:/frontend
      - /frontend/node_modules
    ports:
      - "3042:3000"
    command: dev
    depends_on:
      - backend

  backend:
    build:
      context: ./Back
      dockerfile: Dockerfile
    volumes:
      - ./Back:/backend
      - /backend/node_modules
    ports:
      - "3142:3001"
    command: dev